@page
@model ExamManager.Pages.TestParseModel
@{
    ViewData["Title"] = "Test Parse";
}

<!DOCTYPE html>
<html>
<head>
    <title>Test Parse Câu Hỏi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        .log-item { padding: 5px; border-left: 3px solid #ddd; margin: 5px 0; }
        .log-success { border-color: #28a745; background: #d4edda; }
        .log-warning { border-color: #ffc107; background: #fff3cd; }
        .log-error { border-color: #dc3545; background: #f8d7da; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>🧪 Test Parse Câu Hỏi</h1>
            <a href="/" class="btn btn-secondary">← Quay lại</a>
        </div>

        <div class="alert alert-info">
            <strong>Hướng dẫn:</strong> Upload file để test xem hệ thống có parse đúng câu hỏi không
        </div>

        <form method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label">Upload file test</label>
                <input type="file" class="form-control" name="file" accept=".docx,.pdf,.txt" required>
            </div>
            <button type="submit" class="btn btn-primary">
                <i class="bi bi-play-fill"></i> Test Parse
            </button>
        </form>

        @if (Model.Logs != null && Model.Logs.Any())
        {
            <div class="card mt-4">
                <div class="card-header bg-dark text-white">
                    <h5><i class="bi bi-terminal"></i> Console Log</h5>
                </div>
                <div class="card-body" style="font-family: monospace; font-size: 0.9rem;">
                    @foreach (var log in Model.Logs)
                    {
                        <div class="log-item @log.Type">@log.Message</div>
                    }
                </div>
            </div>
        }

        @if (!string.IsNullOrEmpty(Model.RawText))
        {
            <div class="card mt-4">
                <div class="card-header bg-info text-white">
                    <h5>📄 Text đã trích xuất</h5>
                </div>
                <div class="card-body">
                    <pre style="max-height: 300px; overflow-y: auto; background: #f5f5f5; padding: 15px; border-radius: 5px; white-space: pre-wrap;">@Model.RawText</pre>
                    <p class="mb-0"><strong>Độ dài:</strong> @Model.RawText.Length ký tự</p>
                </div>
            </div>
        }

        @if (Model.Questions != null && Model.Questions.Any())
        {
            <div class="card mt-4 border-success">
                <div class="card-header bg-success text-white">
                    <h5>✅ Thành công! Parse được @Model.Questions.Count câu hỏi</h5>
                </div>
                <div class="card-body">
                    @* Render first up to 3 questions normally *@
                    @for (int i = 0; i < Math.Min(3, Model.Questions.Count); i++)
                    {
                        var q = Model.Questions[i];
                        <div class="border p-3 mb-3 rounded @(i < 3 ? "bg-light" : "")">
                            <h6 class="text-primary mb-2">
                                <i class="bi bi-question-circle"></i> @q.Question
                            </h6>
                            <ul class="list-unstyled ms-3 mb-0">
                                @foreach (var opt in q.Options)
                                {
                                    <li class="mb-1">
                                        <i class="bi bi-check-circle text-success"></i> @opt
                                    </li>
                                }
                            </ul>
                        </div>
                    }

                    @* If there are more than 3, show button and hidden block for the rest *@
                    @if (Model.Questions.Count > 3)
                    {
                        <div class="text-center mb-3">
                            <button class="btn btn-sm btn-outline-secondary" onclick="document.getElementById('moreQuestions').style.display='block'; this.style.display='none'">
                                Xem thêm @(Model.Questions.Count - 3) câu nữa...
                            </button>
                        </div>

                        <div id="moreQuestions" style="display:none;">
                            @for (int i = 3; i < Model.Questions.Count; i++)
                            {
                                var q = Model.Questions[i];
                                <div class="border p-3 mb-3 rounded">
                                    <h6 class="text-primary mb-2">
                                        <i class="bi bi-question-circle"></i> @q.Question
                                    </h6>
                                    <ul class="list-unstyled ms-3 mb-0">
                                        @foreach (var opt in q.Options)
                                        {
                                            <li class="mb-1">
                                                <i class="bi bi-check-circle text-success"></i> @opt
                                            </li>
                                        }
                                    </ul>
                                </div>
                            }
                        </div>
                    }
                </div>
            </div>
        }
        else if (Model.RawText != null)
        {
            <div class="alert alert-warning mt-4">
                <h5><i class="bi bi-exclamation-triangle"></i> Không parse được câu hỏi!</h5>
                <p>Text đã được trích xuất nhưng không tìm thấy câu hỏi hợp lệ.</p>
                <hr>
                <p class="mb-2"><strong>Kiểm tra:</strong></p>
                <ul class="mb-0">
                    <li>Câu hỏi có số thứ tự: <code>1.</code>, <code>2.</code>, <code>3.</code></li>
                    <li>Đáp án có chữ cái: <code>A.</code>, <code>B.</code>, <code>C.</code>, <code>D.</code></li>
                    <li>Mỗi câu hỏi có ít nhất 2 đáp án</li>
                </ul>
            </div>
        }
    </div>
</body>
</html>
